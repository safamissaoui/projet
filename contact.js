function validateForm(e) {
    e.preventDefault()
    var firstname = document.loginForm.name.value
    
   if (name.length==0)
    return alert(`name is required`)
    e.preventDefault()
    var lastname = document.loginForm.name.value
    
   if (name.length==0)
    return alert(`last name is required`)
   
   
   }